DATABASE_URL=sqlite:///./smart_tree.db
JWT_SECRET=trevia-smart-tree-jwt-secret-key-2024
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 系统内置 DashScope（通义千问）配置：用于“使用官方额度（扣积分）”
# 注意：不要把真实密钥提交到 Git；本文件仅示例。
DASHSCOPE_API_KEY=your_dashscope_api_key
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Credits（积分）
NEW_USER_BONUS_POINTS=2000
POINTS_PER_RMB=1000
# 账本签名密钥（缺省则复用 JWT_SECRET）；生产环境建议单独设置并妥善保管
CREDITS_HMAC_SECRET=your_credits_hmac_secret

# 可选：允许 baseUrl 指向 localhost/内网（本地自用/接入本地模型时可设 true）
ALLOW_PRIVATE_LLM_BASEURL=false
